; (ns app.integration.tune_spec
  ; (:use [speclj.core]
        ; [kerodon.core]
        ; [kerodon.test])
  ; (:require [app.app :as app]))

; (describe "visiting the home page"
  ; (it "works"
    ; (-> (session app/handler)
        ; (visit "/")
        ; (has (text? "tunes")))
    ; ))
